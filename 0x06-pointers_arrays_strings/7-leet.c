#include "main.h"

/**
 * leet - mozart music
 * @a: arg1
 * Return: char
 */

char *leet(char *a)
{
	int a[] = {65, 69, 79, 84, 76};
	int b[] = {'4', '3', '0', '7', '1'};
	int i, j;
	int c, d;

	if (a[i] == '\0')
		break;

	c = a[i];
	if (c == 'a' || a[i] == 'A')
	{
		a[i] = '4';
	}

	return (a);
}
